<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/lucide/0.263.1/lucide.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lucide/0.263.1/lucide.min.js"></script>
</head>
<body class="min-h-screen bg-white">
    <!-- Header -->
    <header class="bg-white shadow-md py-4">
        <div class="container mx-auto px-4 flex justify-between items-center">
            <a href="/" class="text-2xl font-bold">
                VỮNG<span class="text-yellow-400">XÂY</span>
            </a>
            <button class="bg-yellow-400 hover:bg-yellow-500 text-slate-800 px-4 py-2 rounded">Get Quote</button>
        </div>
    </header>

    <!-- Page Header -->
    <div class="bg-slate-800 py-16">
        <div class="container mx-auto px-4 text-center">
            <h1 class="text-4xl font-bold text-white mb-4">Contact Us</h1>
            <p class="text-xl text-slate-300 max-w-2xl mx-auto">
                Get in touch with our experts for all your construction material needs
            </p>
        </div>
    </div>

    <div class="container mx-auto px-4 py-16">
        <div class="grid lg:grid-cols-3 gap-12">
            <!-- Contact Form -->
            <div class="lg:col-span-2">
                <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                    <div class="p-6 border-b border-gray-200">
                        <h2 class="text-2xl font-bold text-slate-800">Send us a Message</h2>
                        <p class="text-slate-600 mt-2">Fill out the form below and we'll get back to you as soon as possible.</p>
                    </div>
                    <div class="p-6">
                        <!-- Display Errors -->
                        <% if (errors && errors.length > 0) { %>
                            <div class="bg-red-50 border border-red-200 rounded-md p-4 mb-6">
                                <div class="flex">
                                    <div class="ml-3">
                                        <h3 class="text-sm font-medium text-red-800">
                                            Please correct the following errors:
                                        </h3>
                                        <div class="mt-2 text-sm text-red-700">
                                            <ul class="list-disc pl-5 space-y-1">
                                                <% errors.forEach(error => { %>
                                                    <li><%= error %></li>
                                                <% }); %>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% } %>

                        <form method="POST" action="/contact" class="space-y-6">
                            <div class="grid md:grid-cols-2 gap-6">
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">Full Name *</label>
                                    <input
                                        type="text"
                                        name="name"
                                        required
                                        value="<%= formData.name || '' %>"
                                        placeholder="Enter your full name"
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent"
                                    />
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">Email Address *</label>
                                    <input
                                        type="email"
                                        name="email"
                                        required
                                        value="<%= formData.email || '' %>"
                                        placeholder="Enter your email"
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent"
                                    />
                                </div>
                            </div>

                            <div class="grid md:grid-cols-2 gap-6">
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">Phone Number *</label>
                                    <input
                                        type="tel"
                                        name="phone"
                                        required
                                        value="<%= formData.phone || '' %>"
                                        placeholder="+84 123 456 789"
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent"
                                    />
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">Company Name</label>
                                    <input
                                        type="text"
                                        name="company"
                                        value="<%= formData.company || '' %>"
                                        placeholder="Your company name"
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent"
                                    />
                                </div>
                            </div>

                            <div class="grid md:grid-cols-2 gap-6">
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">Inquiry Type *</label>
                                    <select
                                        name="inquiryType"
                                        required
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent"
                                    >
                                        <option value="">Select inquiry type</option>
                                        <% inquiryTypes.forEach(type => { %>
                                            <option value="<%= type.value %>" <%= formData.inquiryType === type.value ? 'selected' : '' %>>
                                                <%= type.label %>
                                            </option>
                                        <% }); %>
                                    </select>
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">Project Type</label>
                                    <select
                                        name="projectType"
                                        class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent"
                                    >
                                        <option value="">Select project type</option>
                                        <% projectTypes.forEach(type => { %>
                                            <option value="<%= type.value %>" <%= formData.projectType === type.value ? 'selected' : '' %>>
                                                <%= type.label %>
                                            </option>
                                        <% }); %>
                                    </select>
                                </div>
                            </div>

                            <div>
                                <label class="block text-sm font-medium text-slate-700 mb-2">Message *</label>
                                <textarea
                                    name="message"
                                    required
                                    rows="6"
                                    placeholder="Please describe your project requirements, quantities needed, timeline, or any specific questions..."
                                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent resize-vertical"
                                ><%= formData.message || '' %></textarea>
                            </div>

                            <button
                                type="submit"
                                class="w-full bg-yellow-400 hover:bg-yellow-500 text-slate-800 font-semibold py-3 px-4 rounded-md transition-colors duration-200 flex items-center justify-center"
                            >
                                <i data-lucide="send" class="mr-2 h-5 w-5"></i>
                                Send Message
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Contact Information -->
            <div class="space-y-6">
                <% contactInfo.forEach(info => { %>
                    <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                        <div class="p-6">
                            <div class="flex items-center mb-4">
                                <i data-lucide="<%= info.icon %>" class="h-6 w-6 text-yellow-400 mr-3"></i>
                                <h3 class="text-lg font-bold text-slate-800"><%= info.title %></h3>
                            </div>
                            <div class="space-y-3">
                                <% info.details.forEach(detail => { %>
                                    <div>
                                        <div class="text-sm font-medium text-slate-600"><%= detail.label %></div>
                                        <div class="text-slate-800"><%= detail.value %></div>
                                    </div>
                                <% }); %>
                            </div>
                        </div>
                    </div>
                <% }); %>

                <!-- Quick Actions -->
                <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                    <div class="p-6 border-b border-gray-200">
                        <h3 class="text-lg font-bold text-slate-800">Quick Actions</h3>
                    </div>
                    <div class="p-6 space-y-3">
                        <% inquiryTypes.slice(0, 4).forEach(type => { %>
                            <button
                                onclick="document.querySelector('select[name=inquiryType]').value='<%= type.value %>'"
                                class="w-full text-left border border-gray-300 rounded-md px-4 py-2 hover:bg-gray-50 transition-colors duration-200 flex items-center"
                            >
                                <i data-lucide="<%= type.icon %>" class="mr-2 h-4 w-4"></i>
                                <%= type.label %>
                            </button>
                        <% }); %>
                    </div>
                </div>

                <!-- Social Media -->
                <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                    <div class="p-6 border-b border-gray-200">
                        <h3 class="text-lg font-bold text-slate-800">Follow Us</h3>
                    </div>
                    <div class="p-6">
                        <div class="flex space-x-4">
                            <i data-lucide="facebook" class="h-8 w-8 text-blue-600 cursor-pointer hover:text-blue-700 transition-colors"></i>
                            <i data-lucide="instagram" class="h-8 w-8 text-pink-600 cursor-pointer hover:text-pink-700 transition-colors"></i>
                            <i data-lucide="linkedin" class="h-8 w-8 text-blue-700 cursor-pointer hover:text-blue-800 transition-colors"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Map Section -->
        <div class="mt-16">
            <h2 class="text-3xl font-bold text-slate-800 mb-8 text-center">Find Our Locations</h2>
            <div class="grid md:grid-cols-3 gap-8 mb-8">
                <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                    <div class="p-6 text-center">
                        <span class="inline-block bg-yellow-400 text-slate-800 px-3 py-1 rounded-full text-sm font-medium mb-4">Head Office</span>
                        <h3 class="font-bold text-slate-800 mb-2">Ho Chi Minh City</h3>
                        <p class="text-slate-600 text-sm mb-4">123 Construction Street, District 1</p>
                        <button class="border border-gray-300 text-slate-700 px-4 py-2 rounded-md hover:bg-gray-50 transition-colors">
                            Get Directions
                        </button>
                    </div>
                </div>
                <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                    <div class="p-6 text-center">
                        <span class="inline-block bg-slate-600 text-white px-3 py-1 rounded-full text-sm font-medium mb-4">Warehouse</span>
                        <h3 class="font-bold text-slate-800 mb-2">Industrial Zone</h3>
                        <p class="text-slate-600 text-sm mb-4">456 Industrial Zone, District 9</p>
                        <button class="border border-gray-300 text-slate-700 px-4 py-2 rounded-md hover:bg-gray-50 transition-colors">
                            Get Directions
                        </button>
                    </div>
                </div>
                <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                    <div class="p-6 text-center">
                        <span class="inline-block bg-slate-600 text-white px-3 py-1 rounded-full text-sm font-medium mb-4">Branch</span>
                        <h3 class="font-bold text-slate-800 mb-2">Hanoi Office</h3>
                        <p class="text-slate-600 text-sm mb-4">789 Business Center, Hanoi</p>
                        <button class="border border-gray-300 text-slate-700 px-4 py-2 rounded-md hover:bg-gray-50 transition-colors">
                            Get Directions
                        </button>
                    </div>
                </div>
            </div>

            <div class="bg-white shadow-lg rounded-lg border border-gray-200">
                <div class="p-0">
                    <div class="bg-slate-100 h-96 flex items-center justify-center rounded-lg">
                        <div class="text-center text-slate-500">
                            <i data-lucide="map-pin" class="h-16 w-16 mx-auto mb-4"></i>
                            <h3 class="text-xl font-bold mb-2">Interactive Map</h3>
                            <p>Google Maps integration would be embedded here</p>
                            <p class="text-sm mt-2">Showing all VỮNG XÂY locations</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();
        
        // Form submission loading state
        document.querySelector('form').addEventListener('submit', function(e) {
            const submitBtn = e.target.querySelector('button[type="submit"]');
            submitBtn.innerHTML = '<i data-lucide="loader-2" class="mr-2 h-5 w-5 animate-spin"></i>Sending...';
            submitBtn.disabled = true;
        });
    </script>
</body>
</html>